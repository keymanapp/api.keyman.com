{
  "$schema": "http://json-schema.org/schema#",
  "$ref": "#/definitions/version",

  "definitions": {
    "version": {
      "type": "object",
      "properties": {
        "platform": { "type": "string",  "enum": ["windows", "mac", "android", "ios", "web", "linux"] },
        "level": { "type": "string", "enum": ["stable", "alpha", "beta"] },
        "version": { "type": "string" }
      }
    }
  }
}